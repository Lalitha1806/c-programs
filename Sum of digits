// find the sum of the digits of a num
#include <stdio.h>

int main() {
    int n,sum=0,rem;
    printf("enter the digits:");
    scanf("%d",&n);
    while(n>0)
    {
        rem = n%10;
        sum = sum + rem;
        n = n/10;
    }
    printf("the sum of digits:%d",sum);
    return 0;
}
